#include <stdio.h>

int main() {
  int num;   // Initialize a variable
  num = 10;  // Assigning a value
  printf("num is %d\n", num);

  scanf("%d", &num);
  printf("num is %d\n", num);

  return 0;
}
